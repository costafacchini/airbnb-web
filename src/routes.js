import React from "react";
import { BrowserRouter, Switch } from "react-router-dom";

function Routes() {
  return (
    <BrowserRouter>
      <Switch>
      </Switch>
    </BrowserRouter>
  );
}

export default Routes;